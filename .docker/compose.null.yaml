# This file is intentionally empty.
# It is used as a placeholder for Docker Compose when no GPU-specific
# configurations are needed, allowing devcontainer.json to safely
# include it without causing errors on non-NVIDIA systems.
services:
  ros2_rox2026: {}
